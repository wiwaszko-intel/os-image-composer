# AI-searchable metadata for template discovery
metadata:
  description: Minimal Ubuntu 24.04 bootable ISO image for installation media
  use_cases:
    - Creating bootable installation media
    - USB/DVD installation
    - Bare metal provisioning
    - System recovery
  keywords:
    - iso
    - bootable
    - installation
    - usb
    - dvd
    - minimal
    - ubuntu

image:
  name: minimal-os-image-ubuntu
  version: "24.04"

target:
  os: ubuntu
  dist: ubuntu24
  arch: x86_64
  imageType: iso # Image type, valid value: [raw, iso].