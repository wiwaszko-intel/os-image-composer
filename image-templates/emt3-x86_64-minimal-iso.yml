# AI-searchable metadata for template discovery
metadata:
  description: "Edge Microvisor Toolkit 3 minimal ISO installer image for bootable installation media"
  use_cases:
    - "system installation"
    - "bootable media"
    - "USB installer"
    - "bare metal deployment"
  keywords:
    - iso
    - installer
    - bootable
    - minimal
    - emt3
    - microvisor
    - usb

image:
  name: emt3-x86_64-minimal
  version: "1.0.0"

target:
  os: edge-microvisor-toolkit # Target OS name
  dist: emt3 # Target OS distribution
  arch: x86_64 # Target OS architecture
  imageType: iso # Image type, valid value: [raw, iso].